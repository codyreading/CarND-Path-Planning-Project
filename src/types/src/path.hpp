#ifndef PATH_HPP
#define PATH_HPP

#include <vector>

struct Path
{
    std::vector<double> x; // List of x coordinates to define a path
    std::vector<double> y; // List of y coordinates to define a path
};

#endif